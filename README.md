# observable

observable vibe coding

## GitHub Pages

A GitHub Actions workflow deploys the contents of the `observable/` directory to GitHub Pages whenever changes land on the `main` or `work` branches. After pushing, open the Actions tab, locate the "Deploy to GitHub Pages" workflow run, and follow the provided URL to view the published site.
